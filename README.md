> ⚠️ **Moved to the monorepo.**
> This module now lives in [`opstimus/terraform-modules`](https://github.com/opstimus/terraform-modules) at `modules/oci-postgresql`.
>
> ```hcl
> source = "git::https://github.com/opstimus/terraform-modules.git//modules/oci-postgresql?ref=oci-postgresql/v1.0.1"
> ```
>
> This repository remains for existing consumers; new development happens in the monorepo.

# terraform-oci-postgresql
terraform-oci-postgresql
